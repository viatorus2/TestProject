language: node_js
node_js:
    - "8"
sudo: false
branches:
    only:
    - master

 # Run npm test always
script:
    - "npm test"
deploy:
    provider: npm
    email: $NPM_EMAIL
    api_key: $NPM_KEY
    on:
        tags: true
        branch: master
